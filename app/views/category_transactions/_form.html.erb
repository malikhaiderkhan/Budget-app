<%= form_with(model: category_transaction) do |form| %>
  <% if category_transaction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category_transaction.errors.count, "error") %> prohibited this category_transaction from being saved:</h2>

      <ul>
        <% category_transaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.label :money_transaction_id, style: "display: block" %>
    <%= form.text_field :money_transaction_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
