<div class="col-lg-6 col-md-12 mx-auto flex flex-column p-2 h-100">
<%= form_with(model: category) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :name, placeholder: "Name", required: true, class: "form-control w-100 rounded-md p-3 border focus:outline-none mt-4" %>
  </div>

  <div>
    <%= form.label :icon, style: "display: block", class: "mx-3 pt-3 font-weight-medium fs-5" %>
    <%= form.file_field :icon, required: true, class:"w-100 rounded-md p-3 focus-outline-none" %>
  </div>

  <div>
    <%= form.submit "Save", class: 'bg-primary text-white text-center w-100 p-2 rounded mt-4' %>
  </div>
<% end %>
</div>